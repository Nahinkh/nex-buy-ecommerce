import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
   images: {
    domains: ['res.cloudinary.com','raw.githubusercontent.com','images.unsplash.com'],
  },
};

export default nextConfig;
